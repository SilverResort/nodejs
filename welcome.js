const calcule=require('./localModule.js')
let a=3
let b=5

console.log(`la somme de ${a} + ${b} est`, calcule.somme(a,b))

console.log(`la soustraction de ${a} + ${b} est`, calcule.soustractions(a,b))